PORT=3000
APP_BASE_URL=http://localhost:3000
SESSION_DAYS=7
MAIL_FROM="TUNIFIX <no-reply@tunifix.tn>"
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
# Set to true if you want the server to fail when email sending fails
MAIL_REQUIRED=false
